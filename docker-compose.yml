version: "3.9"

services:
  app:
    image: highjin/discord-bot:latest
    env_file:
      - .env
    volumes:
      - ./src/jsons/:/app/jsons/
    networks:
      - local

networks:
  local:
    name: local
    external: false
